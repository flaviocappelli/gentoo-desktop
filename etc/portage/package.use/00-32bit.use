# by F.C.

# Ebuilds app-emulation/wine-* 11.0 and above, with the +wow64 use flag
# (enabled by default) no longer require 32-bit packages! Wine now works
# even in no-multilib profile (see https://wiki.gentoo.org/wiki/Wine).
