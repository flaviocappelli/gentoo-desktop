# by F.C.
#
# These packages might fail due to incorrect parallel make.
#
# Note: settings in /etc/portage/package.env/* are applied
# only when portage is actually processing the ebuild using
# bash, and that isn't done when using 'emerge --info'. Use
# script "/etc/portage/bashrc" to debug emerge variables.

app-text/texlive-core           noparallelmake.conf
media-fonts/monafont            noparallelmake.conf
media-fonts/opendesktop-fonts   noparallelmake.conf
media-libs/embree               noparallelmake.conf
net-libs/signon-ui              noparallelmake.conf
