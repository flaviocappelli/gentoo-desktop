# by F.C.
#
# These are temporary fix (will be removed in the future).

# NOTE: on any issue with VirtualBox >= 7.1.8 remove
# ~/.VirtualBox and reimport all VMs (recent versions
# have configuration in ~/.config/VirtualBox).

# Do not pull in Python 3.14 until really required.
>=dev-lang/python-3.14.0

# Delay the upgrade to kernel 6.18.x LTS, because
# required out-of-tree drivers are not ready yet.
>=virtual/dist-kernel-6.18.0
