# by F.C.
#
# These are temporary fix (will be removed in the future).

# Finally I solved the VirtualBox issue: v7.1.8 works
# but I had to remove ~/.VirtualBox and reimport all VMs
# (note: now the configuration is in ~/.config/VirtualBox).

# FreeCAD <= 1.0.1 needs this (see the ebuild).
>=dev-python/pybind11-3.0.0
