# by F.C.
#
# By default, packages from additional repos (!gentoo, !local) are disabled
# (see /etc/portage/package.accept_keywords/00-other_repos.accept_keywords).
# Since they cannot be installed unless explicitly enabled, there is no need
# to mask packages here.
